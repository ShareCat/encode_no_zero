## 128 进制映射（255 进制大整数映射）

核心原理（一句话）
把原始字节流看成一个大整数，把它转换成 255 进制，每个数位 +1，这样输出字节范围就是 0x01 ~ 0xFF，绝对没有 0x00。
解码就是反向操作：每个字节 -1 → 转回 255 进制 → 还原原始字节流。

特点总结
1、输出字节：0x01 ~ 0xFF（无 0x00）
2、空间膨胀最小（几乎接近 0）
3、适合：文件存储、高压缩需求、嵌入式存储
4、纯 C 无依赖，可跑单片机

和COBS对比
COBS：速度最快，适合串口通信
255 进制映射：空间最小，适合存储 / 压缩
